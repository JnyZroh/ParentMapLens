# Core API framework — fast, async-ready, auto-generates /docs (Swagger UI)
fastapi==0.115.6

# ASGI server — runs FastAPI in development mode with --reload hot-reloading
uvicorn[standard]==0.34.0

# ORM for SQLite — lets us define database tables as Python classes
sqlalchemy==2.0.36

# Pydantic v2 is bundled with FastAPI for request/response validation
# (no explicit pin needed — FastAPI pulls the right version)
